export type TestSettings = {
  typeService: number;
  serviceId: number;
  siteId: number;
  count: number;
};

export type ExtraSettings = {
  serviceId: number;
  siteId: number;
};

export type TGSenderCommentInfo = {
  message: string;
};
